apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ophthalmoai
spec:
  selector:
    app: ophthalmoai
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
