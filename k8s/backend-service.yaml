apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ophthalmoai
spec:
  selector:
    app: ophthalmoai
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
